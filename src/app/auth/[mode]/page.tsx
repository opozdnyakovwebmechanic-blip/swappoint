"use client"; import {Layout} from "@/components/app"; import {useParams,useRouter} from "next/navigation"; export default function Page(){const p=useParams<{mode:string}>(),r=useRouter(),login=p.mode==="login";return <Layout><section className="auth"><p className="eyebrow">SwapPoint</p><h1>{login?"С возвращением":"Создайте аккаунт"}</h1><p>{login?"Войдите, чтобы продолжить обмениваться.":"Начните давать вещам вторую жизнь."}</p><form onSubmit={e=>{e.preventDefault();r.push("/profile")}}>{!login&&<input placeholder="Ваше имя"/>}<input type="email" placeholder="Email" required/><input type="password" placeholder="Пароль" required/><button className="btn">{login?"Войти":"Зарегистрироваться"}</button></form><p>{login?"Нет аккаунта? ":"Уже есть аккаунт? "}<a href={login?"/auth/register":"/auth/login"}>{login?"Регистрация":"Войти"}</a></p></section></Layout>}
